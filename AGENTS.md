Use strong branded types. `src/domain.ts` defines the brand helper.
Target the globally installed Pi runtime; never hard-code specific Pi release versions in source, tests, or documentation.
Before changing code, read `docs/README.md` and follow its task-specific route.
Before committing, follow the applicable gates in `docs/verification.md`.
